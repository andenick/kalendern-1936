This image is a single vertical column from a 1936 Swedish printed tax directory (Antiqua/Latin type, not Fraktur). Transcribe ALL text exactly as printed, line by line, top to bottom. Preserve every Swedish character (å, ä, ö, Å, Ä, Ö), every dash/en-dash (–), comma, period, and digit exactly. Preserve abbreviations and initials as printed. Do NOT translate, expand, reorder, summarize, or add commentary. Output ONLY the raw transcription, one source line per output line.
