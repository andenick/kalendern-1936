Transcribe ALL text in this image exactly, line by line, from top to bottom. Output PLAIN TEXT ONLY — absolutely NO HTML, no <table>, <tr> or <td> tags, no markdown. Put each directory entry on its OWN line (a newline after every entry). Preserve every Swedish character (å, ä, ö, Å, Ä, Ö), every en-dash/em-dash (–) between income figures, every comma and digit. Do NOT repeat any line. Do NOT loop. Output only the transcription, nothing else.
