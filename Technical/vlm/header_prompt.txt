This image is the running header strip from the top of a page in a 1936 Swedish printed tax directory (Antiqua/Latin type). It contains a page number (a 1-3 digit number, usually at one edge), a running header (often a surname range such as "ADE—AHL" or a section/place label), and possibly the year 1936. Transcribe everything exactly as printed on one line, preserving Swedish characters and digits. Output ONLY the transcription, nothing else.
